#include <stdio.h>
/**
 * main - uses function getchar to print the alphabet
 * Return: always zero
 */

int main(void)
{
int n;
for (n = 1 ; n <= 9 ; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
